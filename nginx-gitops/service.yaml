apiVersion: v1
kind: Service
metadata:
  name: nginx-service
 # namespace: dev   # <-- Add this if your Deployment is in the 'dev' namespace
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
